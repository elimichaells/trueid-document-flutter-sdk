// TrueID Document SDK for Flutter — standard document verification:
// front/back card (or passport data page) capture, additional info, and a
// trailing selfie+liveness step. Separate from `trueid_sdk` (PIN+selfie),
// but shares its `TrueIdSdk.initialize()` call — see that package's docs.
//
// final result = await TrueIdDocumentSdk.verifyDocument();

export 'src/trueid_document_sdk.dart';
export 'src/models.dart';
